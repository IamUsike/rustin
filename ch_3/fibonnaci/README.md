Challenge 7: Fibonacci Sequence [Medium]

Write two functions: fib_iterative(n: u64) -> u64 and fib_recursive(n: u64) -> u64. Print the first 20 Fibonacci numbers using both. Add a constant MAX_FIB: u64 = 12586269025 and validate results against it.

Hints: recursive will be slow for large n — that’s okay here • use const for the constant.

- finding that Fibonacci im not jacruzzi. arghhhhhhh
