no way im printing any pyramids (skip)

Challenge 8: Statistics Calculator [Medium]

Write a function that takes an array of integers and returns (min, max, mean, and the range). Use a tuple as return type. Call it with at least 3 different fixed arrays and print nicely formatted results.

Hints: return (min, max, mean, range) as a tuple - use i32::MIN and i32::MAX as initial values
