**Challenge 11: Word Counter [Easy]**

Write a function `count_words(s: &str) -> usize` that counts words in a string slice. It should handle multiple spaces, leading/trailing spaces. Test it with at least 5 different inputs including edge cases (empty string, single word).

**Hints:** use `split_whitespace()` • collect into a `Vec` then `.len()`
