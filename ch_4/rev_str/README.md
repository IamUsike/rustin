**Challenge 14: String Reversal In-Place**

Write `reverse_words(s: &str) -> String` that reverses the order of words in a sentence (not the characters).
Example: `"hello world"` becomes `"world hello"`.
Handle multiple spaces correctly.

**Hints:** collect words into `Vec<&str>` • `.rev()` on iterator
